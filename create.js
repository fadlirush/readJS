const express=require("express");const bcrypt=require('bcrypt');const app=express();const password='intan';const saltRounds=10;bcrypt.hash(password,saltRounds,(err,hash)=>{if(err){console.error('Error hashing password:', err);return;}console.log('Hashed password:', hash);});const PORT=process.env.PORT || 8839;app.listen(PORT,()=>{console.log(`Server is running on port ${PORT}`);});